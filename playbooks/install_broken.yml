- name: Install nginx the wrong way
  hosts: servers
  become: true
  tasks:
    - name: Install nginx (breaking idempotence deliberately)
      ansible.builtin.command:
        cmd: apt install -y nginx
        creates: /usr/sbin/nginx
